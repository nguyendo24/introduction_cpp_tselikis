#include <iostream> // ’σκηση 8.3
int main()
{
	int *p, i;

	p = &i;
	*p = 1;
	while(*p <= 10)
	{
		std::cout << *p << '\n';
		(*p)++; /* Όπως θα δείτε στη συνέχεια, πρέπει να χρησιμοποιήσουμε παρενθέσεις για λόγους προτεραιότητας. */
	}
	return 0;
}

